sudo: required
services:
  - redis-server

language: go

go:
  - 1.18
  - 1.20
  - tip

install:
  - go get -t -v ./...
  - go get github.com/mattn/goveralls

script:
  - goveralls -service travis-ci
